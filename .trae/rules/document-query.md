---
alwaysApply: false
description: 文档查询强制约束，适用于所有涉及 API、组件、配置的场景
scene: coding
---

# 文档查询

- 不确定 API / 组件 / 配置行为时，必须先查询最新文档，不得依赖训练数据
- 库版本较新时，不得凭记忆写代码，必须查询文档确认当前版本用法
- 配置不生效时，必须先查官方文档确认用法，不得猜测
- 引入新依赖前，必须先确认兼容性
- 涉及 Supabase 或 Nuxt UI 的问题，必须先使用专用 MCP 查询
- 查询结果与记忆冲突时，以文档为准
