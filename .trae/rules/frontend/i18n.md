---
alwaysApply: false
description: 国际化约束，涉及多语言文本、翻译配置时生效
---

- 文本通过 `$t('key')` 或 `useI18n()` 的 `t` 函数读取，不硬编码
- 翻译 key 在 `@supabase/i18n` 包的对应语言文件中维护
- 新增文本时，同步补充所有 4 种语言（zh-CN / en / ja / ko）
- 翻译 key 使用点号命名空间：`home.title`、`project.list.empty`
